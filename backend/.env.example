SUPABASE_URL=
SUPABASE_ANON_KEY=
FRONTEND_ORIGIN=http://localhost:5173

# Supabase PostgreSQL connection string.
# IMPORTANT: Keep this only in backend/.env (never in frontend files).
DATABASE_URL=

# Optional raw password field for convenience/reference in backend scripts.
# Keep this only in backend/.env and do not expose it in frontend code.
DATABASE_PWD=
